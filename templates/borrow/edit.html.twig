{% extends 'base.html.twig' %}

{% block title %}Edit Borrow{% endblock %}

{% block body %}
    <h1>Edit Borrow</h1>

    {{ include('borrow/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('borrow_index') }}">back to list</a>

    {{ include('borrow/_delete_form.html.twig') }}
{% endblock %}
